use("Inv_Agaval")

// Eliminar un producto por id
db.products.deleteOne({_id: 1})

// Eliminar varios productos por una condición. 
db.products.deleteMany({price:100})

// Eliminar todos los documentos de la colección. 
db.products.drop()